# Signing material and credentials must never be committed.
*.jks
*.keystore
RELEASE-KEY-CREDENTIALS.txt

# Build output (regenerated by the build scripts).
build/
dist/
release-assets/
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/keystore.properties

.vercel
